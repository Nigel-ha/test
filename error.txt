Requested attribute CanonicalHostedZoneId does not exist in schema for AWS::ElasticLoadBalancingV2::LoadBalancer


  Route53RecordA:
    Type: AWS::Route53::RecordSet
    Properties:
      HostedZoneId: !ImportValue SCE-PRIVATE-HOST-ID
      Name: !Sub "${ClusterName}-k8s-api.${SpokeDomain}"
      Type: A
      AliasTarget:
        DNSName: !GetAtt NLB.DNSName
        HostedZoneId: !GetAtt NLB.CanonicalHostedZoneId
      Comment: "k8s API"


TARGET_GROUP_ARN=$(aws elbv2 describe-target-groups \
  --names "${CLUSTER_NAME}-nlb-tg" \
  --query "TargetGroups[0].TargetGroupArn" \
  --output text)
